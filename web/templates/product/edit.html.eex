<h2>Edit product</h2>

<%= render "form.html", changeset: @changeset, category_select: @category_select,
                        action: product_path(@conn, :update, @product) %>

<%= link "Back", to: product_path(@conn, :index) %>
